#pragma once
#include <string>
#include <vector>
#include <iostream>
using namespace std;

void readfile(string, vector<int>&);
void changeValue(vector<int>&);
void addValue(vector<int>&);
void printArray(vector<int>&);
bool isDigits(const string&);