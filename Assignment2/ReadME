For the classes, an instance is created and the user is allowed to input values. The code will go through each function and output the results.
the modified funtions from last weeks assignment work the same as before, except with some error checking this time around
